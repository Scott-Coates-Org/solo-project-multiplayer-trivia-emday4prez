/* I exist solely to ensure that the database keys are configured. You're free to delete me. */
import firebase from '../firebase/client';

function DbTest() {
    return (
        <div className='DbTest'>
            hello
        </div>
    )
}

export default DbTest
